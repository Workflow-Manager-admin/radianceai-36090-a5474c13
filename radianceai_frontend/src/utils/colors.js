/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}//**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}//**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}P/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}U/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}B/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}L/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}I/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}C/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}_/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}I/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}N/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}T/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}E/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}R/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}F/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}A/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}C/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}E/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}
/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}//**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}//**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}T/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}h/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}e/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}m/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}e/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}c/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}o/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}l/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}o/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}r/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}p/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}a/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}l/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}e/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}t/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}t/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}e/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}f/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}o/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}r/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}R/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}a/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}d/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}i/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}a/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}n/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}c/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}e/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}A/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}I/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}(/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}D/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}A/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}R/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}K/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}p/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}a/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}l/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}e/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}t/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}t/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}e/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
})/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}
/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}e/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}x/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}p/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}o/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}r/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}t/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}c/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}o/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}n/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}s/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}t/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}C/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}O/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}L/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}O/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}R/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}S/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}=/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}{/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}
/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}p/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}r/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}i/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}m/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}a/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}r/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}y/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}:/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}'/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}#/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}2/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}1/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}2/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}3/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}2/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}9/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}'/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
},/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}//**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}//**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}d/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}a/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}r/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}k/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}g/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}r/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}e/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}y/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}
/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}s/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}e/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}c/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}o/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}n/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}d/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}a/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}r/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}y/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}:/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}'/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}#/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}F/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}F/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}D/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}4/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}4/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}7/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}'/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
},/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}//**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}//**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}d/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}e/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}e/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}p/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}y/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}e/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}l/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}l/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}o/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}w/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}a/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}c/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}c/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}e/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}n/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}t/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}
/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}a/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}c/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}c/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}e/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}n/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}t/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}:/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}'/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}#/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}1/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}8/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}2/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}C/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}4/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}7/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}'/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
},/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}//**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}//**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}n/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}a/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}v/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}y/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}b/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}l/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}u/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}e/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}p/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}r/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}i/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}m/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}a/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}r/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}y/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
} /**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}t/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}e/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}x/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}t/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}
/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}}/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
};/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}
/**
 * Color palette and utility functions for RadianceAI.
 * All legacy pink/purple colors are replaced with the new palette: blue, light blue, white, beige, creamy white.
 */

export const PALETTE = {
  blue:      "#2050aa",
  blueLight: "#77a6ed",
  blueDark:  "#182349",
  blueDeep:  "#1663b7",
  blueBright:"#2e6ff2",
  white:     "#fff",
  beige:     "#f6efe3",
  creamy:    "#FFF8EB",
  border:    "#dde9ff",
  blueSoft:  "#ebf4ff",
  blueMuted: "#eaf5ff",
};

export const GRADIENTS = {
  primary:     `linear-gradient(91deg, ${PALETTE.blueBright} 49%, ${PALETTE.blueLight} 100%)`,
  secondary:   `linear-gradient(92deg, ${PALETTE.blueLight} 60%, ${PALETTE.blue} 100%)`,
  bgHero:      `linear-gradient(98deg, ${PALETTE.blueDark} 77%, #e2f0fa14 100%)`,
  card:        `linear-gradient(104deg,${PALETTE.blueSoft} 60%,${PALETTE.border} 100%)`,
  modal:       `linear-gradient(101deg, ${PALETTE.white} 0%, ${PALETTE.beige} 60%, ${PALETTE.creamy} 100%)`,
  overlay:     `rgba(36,58,125,0.23)`,
  input:       `${PALETTE.blueMuted}`,
};

export function themeShadow(level = 1) {
  switch (level) {
    case 2: return "0 2px 18px #a8d4fc44";
    case 3: return "0 8px 48px 0 #3388e726, 0 2px 6px #bcdcfd42";
    default: return "0 1.5px 7px #a2c5e844";
  }
}