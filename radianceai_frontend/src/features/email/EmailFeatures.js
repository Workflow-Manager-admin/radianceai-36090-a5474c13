import React from "react";

// PUBLIC_INTERFACE
/**
 * Email reminders and summaries feature placeholder.
 */
const EmailFeatures = () => (
  <section className="container">
    <h2>Email Reminders & Summaries (Placeholder)</h2>
    <p>Email functionality using EmailJS will appear here.</p>
  </section>
);

export default EmailFeatures;
