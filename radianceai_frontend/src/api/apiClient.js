// PUBLIC_INTERFACE
/**
 * apiClient: sets up fetch calls for different APIs.
 * Update with actual endpoints and logic.
 */
export const fetchProducts = async () => {
  // Placeholder for product API
  return [];
};

export const fetchWeather = async (lat, lon) => {
  // Placeholder for weather API
  return {};
};

export const sendEmail = async (payload) => {
  // Placeholder for email sending (EmailJS)
  return true;
};
